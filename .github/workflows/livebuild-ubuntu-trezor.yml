name: Live-Build Ubuntu + Trezor Suite ISO

on:
  workflow_dispatch:
    inputs:
      ubuntu_codename:
        description: "Ubuntu codename: noble (24.04 LTS) or jammy (22.04 LTS)"
        default: "noble"
        required: true
      disable_network_by_default:
        description: "Disable NetworkManager by default in the live session"
        type: boolean
        default: false
      trezor_tag:
        description: "Trezor Suite release tag (e.g., v25.9.3). Leave blank for latest"
        default: ""
        required: false
  schedule:
    - cron: "0 9 1 * *"  # monthly, 09:00 UTC on the 1st

permissions:
  contents: write

jobs:
  live_build:
    runs-on: ubuntu-22.04
    timeout-minutes: 120
    defaults:
      run:
        shell: bash
    env:
      # Mirrors
      UBUNTU_MIRROR: "http://archive.ubuntu.com/ubuntu/"
      UBUNTU_SECURITY: "http://security.ubuntu.com/ubuntu/"
      # trezor-suite repo
      GH_OWNER: "trezor"
      GH_REPO: "trezor-suite"
      # Output ISO name
      TAILORED_ISO_NAME: "ubuntu-live-trezor.iso"
      # Ensure no syslinux theme is ever requested
      LB_SYSLINUX_THEME: "none"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Runner info (debug trap enabled)
        run: |
          set -Eeuo pipefail
          trap 'CODE=$?; echo "::group::DEBUG FAIL CONTEXT"; echo "Exit: $CODE"; echo "PWD: $(pwd)"; echo "Tree (top dirs):"; find -maxdepth 3 -type d -printf "%p\n" || true; echo "Files under config/: "; find config -type f -maxdepth 4 -print 2>/dev/null || true; echo "::endgroup::"; exit $CODE' ERR
          uname -a || true
          lsb_release -a || true
          df -h || true
          free -h || true
          echo "Inputs: codename=${{ github.event.inputs.ubuntu_codename }}, disable_net=${{ github.event.inputs.disable_network_by_default }}, trezor_tag=${{ github.event.inputs.trezor_tag }}"

      - name: Install build tools
        run: |
          set -euxo pipefail
          sudo apt-get update
          # Do not install syslinux-utils/isolinux (we avoid syslinux entirely)
          sudo apt-get install -y \
            live-build debootstrap \
            xorriso squashfs-tools genisoimage \
            dosfstools gpg wget curl rsync ca-certificates \
            ubuntu-keyring jq
          live-build --version || true
          jq --version

      - name: Resolve variables
        id: vars
        run: |
          set -euxo pipefail
          CODENAME="${{ github.event.inputs.ubuntu_codename || 'noble' }}"
          DISABLE_NET="${{ github.event.inputs.disable_network_by_default == 'true' && 'true' || 'false' }}"
          TAG_INPUT="${{ github.event.inputs.trezor_tag }}"
          echo "CODENAME=$CODENAME"            >> "$GITHUB_OUTPUT"
          echo "DISABLE_NET=$DISABLE_NET"      >> "$GITHUB_OUTPUT"
          echo "VOL=UBUNTU_${CODENAME}_TREZOR" >> "$GITHUB_OUTPUT"
          echo "TAG_INPUT=$TAG_INPUT"          >> "$GITHUB_OUTPUT"

      - name: Purge any lb caches/state (prevents old distro fallback)
        run: |
          set -euxo pipefail
          sudo lb clean --purge || true
          sudo rm -rf cache chroot binary local || true
          rm -rf config build-cache logs gnupg || true
          mkdir -p config/includes.chroot/opt/trezor
          mkdir -p config/includes.chroot/usr/share/applications
          mkdir -p config/includes.chroot/etc/udev/rules.d
          mkdir -p config/package-lists
          mkdir -p config/hooks/normal
          mkdir -p logs
          mkdir -p gnupg && chmod 700 gnupg

      # ---- Write files (separate steps to avoid heredoc pitfalls) ----
      - name: Write package list (base.list.chroot)
        run: |
          set -euxo pipefail
          cat > config/package-lists/base.list.chroot <<'EOF'
          ubuntu-standard
          casper
          linux-generic
          network-manager
          xorg
          xserver-xorg
          libfuse2
          xdg-utils
          gvfs
          udev
          EOF

      - name: Write udev rules (51-trezor.rules)
        run: |
          set -euxo pipefail
          cat > config/includes.chroot/etc/udev/rules.d/51-trezor.rules <<'EOF'
          SUBSYSTEM=="hidraw", ATTRS{idVendor}=="534c", MODE="0666"
          SUBSYSTEM=="usb",   ATTR{idVendor}=="534c", MODE="0666"
          EOF

      - name: Write desktop launcher (trezor-suite.desktop)
        run: |
          set -euxo pipefail
          cat > config/includes.chroot/usr/share/applications/trezor-suite.desktop <<'EOF'
          [Desktop Entry]
          Name=Trezor Suite
          Exec=/opt/trezor/trezor-suite.AppImage
          Icon=utilities-terminal
          Type=Application
          Categories=Finance;Utility;
          Terminal=false
          EOF

      - name: Optionally disable networking by default
        if: ${{ steps.vars.outputs.DISABLE_NET == 'true' }}
        run: |
          set -euxo pipefail
          mkdir -p config/includes.chroot/etc/NetworkManager/conf.d
          cat > config/includes.chroot/etc/NetworkManager/conf.d/00-offline.conf <<'EOF'
          [main]
          networking=false
          EOF

      - name: Hook to refresh initramfs
        run: |
          set -euxo pipefail
          cat > config/hooks/normal/99-update-initramfs.chroot <<'EOF'
          #!/bin/sh
          set -eux
          update-initramfs -u
          EOF
          chmod +x config/hooks/normal/99-update-initramfs.chroot

      - name: Debug print written files
        run: |
          set -euxo pipefail
          echo "::group::base.list.chroot"; nl -ba config/package-lists/base.list.chroot; echo "::endgroup::"
          echo "::group::udev 51-trezor.rules"; nl -ba config/includes.chroot/etc/udev/rules.d/51-trezor.rules; echo "::endgroup::"
          echo "::group::desktop trezor-suite.desktop"; nl -ba config/includes.chroot/usr/share/applications/trezor-suite.desktop; echo "::endgroup::"
          if [[ "${{ steps.vars.outputs.DISABLE_NET }}" == "true" ]]; then
            echo "::group::NetworkManager default-off"; nl -ba config/includes.chroot/etc/NetworkManager/conf.d/00-offline.conf; echo "::endgroup::"
          fi

      # ---- Discover AppImage + .asc from GitHub Releases (latest or tag) ----
      - name: Discover Trezor Suite release assets via GitHub API
        id: ghassets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euxo pipefail
          owner="${GH_OWNER}"
          repo="${GH_REPO}"
          tag_input="${{ steps.vars.outputs.TAG_INPUT }}"
          if [[ -n "$tag_input" ]]; then
            api_url="https://api.github.com/repos/${owner}/${repo}/releases/tags/${tag_input}"
          else
            api_url="https://api.github.com/repos/${owner}/${repo}/releases/latest"
          fi
          curl -sSL -H "Authorization: Bearer ${GH_TOKEN}" "$api_url" -o release.json
          echo "::group::Release metadata"
          jq -r '.tag_name, .name, (.assets[]?.name // empty)' release.json || true
          echo "::endgroup::"
          appimage_url=$(jq -r '.assets[] | select(.name|test("AppImage$")) | .browser_download_url' release.json | head -n1)
          asc_url=$(jq -r '.assets[] | select(.name|test("AppImage.asc$")) | .browser_download_url' release.json | head -n1)
          if [[ -z "$appimage_url" || -z "$asc_url" ]]; then
            echo "Could not find AppImage/.asc assets in the release. Assets:"; jq -r '.assets[]?.name' release.json; exit 1
          fi
          echo "APPIMAGE_URL=$appimage_url" >> "$GITHUB_OUTPUT"
          echo "ASC_URL=$asc_url"         >> "$GITHUB_OUTPUT"

      - name: Download AppImage + signature (GitHub Releases)
        run: |
          set -euxo pipefail
          mkdir -p build-cache
          curl -L "${{ steps.ghassets.outputs.APPIMAGE_URL }}" -o build-cache/trezor-suite.AppImage
          curl -L "${{ steps.ghassets.outputs.ASC_URL }}"      -o build-cache/trezor-suite.AppImage.asc
          ls -lh build-cache/

      # ---- Import SatoshiLabs 2021 signing key from trezor.io (HTTPS) ----
      - name: Import Trezor public key (HTTPS from trezor.io)
        env:
          GNUPGHOME: ${{ github.workspace }}/gnupg
        run: |
          set -euxo pipefail
          mkdir -p build-cache/keys
          urls=(
            "https://trezor.io/security/satoshilabs-2021-signing-key.asc"
            "https://trezor.io/security/satoshilabs-2021-signing-key.txt"
            "https://trezor.io/security/satoshilabs-signing-key-2021.asc"
          )
          for u in "${urls[@]}"; do
            if curl -fsSL "$u" -o build-cache/keys/trezor.asc; then
              echo "Downloaded signing key from: $u"
              break
            fi
          done
          if [[ ! -s build-cache/keys/trezor.asc ]]; then
            echo "Failed to download SatoshiLabs 2021 signing key from trezor.io"; exit 2
          fi
          echo "::group::GPG show-keys (import candidate)"
          gpg --show-keys --with-fingerprint build-cache/keys/trezor.asc || true
          echo "::endgroup::"
          gpg --import build-cache/keys/trezor.asc

      # ---- Verify signature and match fingerprint to imported key ----
      - name: Verify AppImage signature (issuer must equal imported key)
        env:
          GNUPGHOME: ${{ github.workspace }}/gnupg
        run: |
          set -euxo pipefail
          IMPORTED_FPR="$(gpg --with-colons --fingerprint | awk -F: '/^fpr:/{print $10; exit}')"
          [ -n "$IMPORTED_FPR" ] || { echo "No imported key fingerprint found"; exit 3; }
          echo "Imported Trezor key fingerprint: $IMPORTED_FPR"

          gpg --status-fd=1 --batch --verify \
            build-cache/trezor-suite.AppImage.asc build-cache/trezor-suite.AppImage \
            2> >(tee logs/gpg-stderr.log >&2) \
            | tee logs/gpg-status.log

          # $1='[GNUPG:]', $2='VALIDSIG'/'GOODSIG', $3=<fingerprint or keyid>
          SIG_GOOD="$(awk 'BEGIN{c=0} /^\[GNUPG:\] GOODSIG /{c++} END{print c}' logs/gpg-status.log)"
          SIG_FPR="$(awk '/^\[GNUPG:\] VALIDSIG /{print $3; exit}' logs/gpg-status.log)"
          SIG_KEYID="$(awk '/^\[GNUPG:\] GOODSIG /{print $3; exit}' logs/gpg-status.log || true)"

          echo "GOODSIG count: ${SIG_GOOD:-0}"
          echo "GOODSIG keyid: ${SIG_KEYID:-unknown}"
          echo "VALIDSIG fingerprint: ${SIG_FPR:-missing}"

          [ "${SIG_GOOD:-0}" -ge 1 ] || { echo "Missing GOODSIG"; exit 4; }
          [ -n "${SIG_FPR:-}" ]      || { echo "Missing VALIDSIG fingerprint"; exit 4; }

          if [ "$SIG_FPR" != "$IMPORTED_FPR" ]; then
            echo "Mismatch: signature issuer ($SIG_FPR) != imported key ($IMPORTED_FPR)"; exit 5
          fi

          chmod +x build-cache/trezor-suite.AppImage
          cp build-cache/trezor-suite.AppImage config/includes.chroot/opt/trezor/trezor-suite.AppImage
          echo "::group::Verify AppImage presence"; ls -l config/includes.chroot/opt/trezor/; echo "::endgroup::"

      - name: Configure live-build (debootstrap; no memtest; no syslinux theme)
        env:
          LB_SYSLINUX_THEME: ${{ env.LB_SYSLINUX_THEME }}
        run: |
          set -euxo pipefail
          CODENAME="${{ steps.vars.outputs.CODENAME }}"
          sudo lb clean --purge || true

          lb config \
            --mode ubuntu \
            --distribution "${CODENAME}" \
            --bootstrap debootstrap \
            --binary-images iso-hybrid \
            --memtest none \
            --syslinux-theme "${LB_SYSLINUX_THEME}" \
            --archive-areas "main restricted universe multiverse" \
            --parent-mirror-bootstrap "${UBUNTU_MIRROR}" \
            --parent-mirror-chroot "${UBUNTU_MIRROR}" \
            --parent-mirror-chroot-security "${UBUNTU_SECURITY}" \
            --parent-mirror-binary "${UBUNTU_MIRROR}" \
            --parent-mirror-binary-security "${UBUNTU_SECURITY}" \
            --apt-recommends true \
            --apt-secure true \
            --debian-installer false \
            --bootappend-live "boot=casper quiet splash" \
            --linux-flavours "generic" \
            --iso-application "Ubuntu Live (Trezor Suite)" \
            --iso-volume "${{ steps.vars.outputs.VOL }}" \
            --checksums "sha256"

          # Env overrides for older lb variants
          cat > config/environment_chroot <<EOF
          LB_DISTRIBUTION=${CODENAME}
          LB_BOOTSTRAP=debootstrap
          LB_MEMTEST=none
          LB_SYSLINUX_THEME=${LB_SYSLINUX_THEME}
          LB_PARENT_MIRROR_BOOTSTRAP=${UBUNTU_MIRROR}
          LB_PARENT_MIRROR_CHROOT=${UBUNTU_MIRROR}
          LB_PARENT_MIRROR_CHROOT_SECURITY=${UBUNTU_SECURITY}
          LB_PARENT_MIRROR_BINARY=${UBUNTU_MIRROR}
          LB_PARENT_MIRROR_BINARY_SECURITY=${UBUNTU_SECURITY}
          EOF

          echo "::group::lb config dir"; ls -la config || true; echo "::endgroup::"

      - name: Build ISO (verbose)
        run: |
          set -euxo pipefail
          (time sudo lb build) 2>&1 | tee logs/live-build.log
          echo "::group::List ISOs"; ls -lh *.iso || true; echo "::endgroup::"
          LB_OUT=$(ls -1 *.iso | head -n1)
          cp "${LB_OUT}" "${TAILORED_ISO_NAME}"
          sha256sum "${TAILORED_ISO_NAME}" | tee ubuntu-live-trezor.iso.sha256

      - name: Upload ISO + checksums + logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: livebuild-output
          path: |
            ${{ env.TAILORED_ISO_NAME }}
            ubuntu-live-trezor.iso.sha256
            logs/
          retention-days: 21

      - name: Create Release
        if: ${{ success() }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: livebuild-${{ steps.vars.outputs.CODENAME }}-${{ github.run_id }}
          name: "Live-Build Ubuntu + Trezor Suite (${{ steps.vars.outputs.CODENAME }})"
          files: |
            ${{ env.TAILORED_ISO_NAME }}
            ubuntu-live-trezor.iso.sha256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
